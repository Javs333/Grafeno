# 3-Evaluation standalone runner

Run evaluation only:

```bash
./3-Evaluation/run_evaluation.sh --run-dir data/<your_run_dir>
```

Parameters:
- `--run-dir` (required, must contain `relaxed.cif`)

Notes:
- Loads environment variables from `.env` at project root.

